# 08 - A Few More Commands Worth Knowing

Not covered hands-on in the original session, but each builds directly on
the fundamentals: `git revert`, `git reset`, `git cherry-pick`,
`git rebase`, `git blame`, and `git show`.
