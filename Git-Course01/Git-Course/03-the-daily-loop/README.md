# 03 - The Daily Loop

This is the cycle you repeat for the rest of your Git life:

```
edit -> git status -> git add -> git commit -> git log
```

Covers `git status`, `git add`, `git commit`, `git commit -a`, `git log`, and
`git rm --cached`.
